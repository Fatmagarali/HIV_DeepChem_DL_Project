# Core ML / Deep Learning
deepchem==2.7.1
tensorflow==2.15.0
tf-keras==2.15.1
# torch 2.6.0: fixes CVE heap-overflow, use-after-free, and torch.load RCE (< 2.6.0)
torch==2.6.0

# NOTE: dgl and dgllife are NOT listed here.
# All known versions of dgl (<= 2.4.0) are affected by a Remote Code Execution
# vulnerability via pickle deserialization (no patched version available).
# See requirements-attentivefp.txt for the optional AttentiveFP dependency,
# which must only be used in isolated / trusted environments.

# Scientific computing
scikit-learn==1.3.2
pandas==2.1.4
numpy==1.24.4

# Visualization
matplotlib==3.8.2
seaborn==0.13.1

# API
fastapi==0.109.1
uvicorn[standard]==0.27.0
pydantic==2.6.0
python-dotenv==1.0.1

# Utilities
protobuf>=3.20,<7
rdkit==2023.9.5
